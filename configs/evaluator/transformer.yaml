type: transformer
d_model: 384
layers: 6
heads: 6
dropout: 0.1
pooling: attention
gradient_checkpointing: false
training: {batch_size: 32, gradient_accumulation: 1, learning_rate: 0.0002, weight_decay: 0.01, max_steps: 10000, max_epochs: 20, min_steps: 2000, precision: bf16, checkpoint_interval: 1000, dataloader_workers: 4, prefetch_factor: 2, log_interval: 10}
inference: {batch_size: 32, precision: bf16}
