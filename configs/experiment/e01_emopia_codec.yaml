# @package _global_
defaults:
  - override /data: emopia
  - override /codec: transformer_vae
  - override /independence: none
experiment: {name: e01_emopia_codec, stage: codec}
codec:
  kl: {warmup_steps: 2000}
  training:
    max_steps: 20000
    max_epochs: 200
    warmup_steps: 500
    checkpoint_interval: 250
    validation_interval: 250
